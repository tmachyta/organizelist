import { UserRegistrationRequest } from './user-registration-request';

describe('UserRegistrationRequest', () => {
  it('should create an instance', () => {
    expect(new UserRegistrationRequest()).toBeTruthy();
  });
});
