import { UpdateTaskRequest } from './update-task-request';

describe('UpdateTaskRequest', () => {
  it('should create an instance', () => {
    expect(new UpdateTaskRequest()).toBeTruthy();
  });
});
