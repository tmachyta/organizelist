import { CreateTaskRequest } from './create-task-request';

describe('CreateTaskRequest', () => {
  it('should create an instance', () => {
    expect(new CreateTaskRequest()).toBeTruthy();
  });
});
